# 拾图设计系统

本文件是整个应用的视觉与交互基线。它由用户提供的 Apple 风格参考与 `gpt-taste` 设计工程规则整理而成。新增页面、组件和状态必须先遵守本文件，再考虑局部表现。

## 1. 设计目标

拾图是一款本地图片收藏与设计术语整理工具。界面应像安静、宽阔的个人工作台：应用 UI 保持克制，让用户收藏的图片成为主要色彩和视觉焦点。整体采用大留白、清晰层级、无装饰性阴影、圆润但不幼稚的组件语言。

## 2. 核心原则

- 内容优先：图片、术语、周记录是主角，工具栏和设置退居其次。
- 单一 UI 强调色：应用操作统一使用电光蓝；不使用旧琥珀色作为全局强调色。
- 色彩例外：周手账里的图钉、回形针、胶带和贴纸保持五颜六色。它们属于用户内容和拟物装饰，不受单一 UI 强调色约束。
- 无阴影层级：页面章节依靠白色与浅灰背景交替、尺寸和留白区分。仅浮层、抽屉、拖拽反馈允许极轻的环境阴影。
- 圆角锁定：内容卡片与图片容器 28px；输入框和小控件不低于 10px；主要按钮使用全圆角。
- 动效服务于空间关系：页面切换、图片层级、抽屉和详情图使用连续动效；减少动画开启时全部退化为即时切换。
- 不使用字符或表情充当功能图标，统一使用 Phosphor 图标。

## 3. Design Tokens

```css
:root {
  --color-primary-ink: #1d1d1f;
  --color-mid-gray: #707070;
  --color-deep-gray: #474747;
  --color-hairline: #d6d6d6;
  --color-canvas: #f5f5f7;
  --color-paper: #ffffff;
  --color-cool-wash: #e8e8ed;
  --color-electric-blue: #0071e3;
  --color-link-blue: #0066cc;
  --color-danger: #c9342f;
  --radius-card: 28px;
  --radius-control: 14px;
  --radius-pill: 999px;
  --page-max-width: 1200px;
}
```

深色模式使用接近黑色的 `#000000`、内容面 `#1c1c1e`、次级面 `#2c2c2e`，强调色使用可读性更高的 `#0a84ff`。深色模式仍不引入装饰渐变或彩色 UI。

## 4. 字体

- 显示与标题：Geist、Segoe UI Variable Display、系统无衬线。字重 600–700，紧缩字距。
- 正文与控件：Geist、Segoe UI Variable Text、系统无衬线。常用字号 14–17px。
- 页面标题 40–56px；模块标题 24–32px；正文 15–17px；辅助信息 12–14px。
- 大标题最多 2–3 行，避免窄栏中的长标题墙。

## 5. 全局框架与导航

- 顶部导航高度 64px，使用半透明白色或深色表面与 20px 模糊。
- 左上角菜单、右上角主题切换使用同一套 40px 圆形图标按钮。
- 左侧抽屉包含“全部图片、周手账、数据与设置”，宽度约 320px。
- 抽屉关闭后画布占满窗口；当前页面使用浅灰选中面与蓝色图标，不使用彩色大块背景。

## 6. 页面规则

### 全部图片

- 搜索栏占满菜单与主题按钮之间的可用宽度；日间使用白色表面与深色内容，夜间使用深灰表面与浅色内容，均保持全圆角。
- 搜索栏内的筛选与导入操作只显示 Phosphor 图标，通过悬停提示和无障碍名称说明功能，不显示常驻文字。
- 夜间模式的空间场景使用纯黑背景，不使用蓝色、蓝灰渐变或底部雾光。
- 图片集中在单个视口的连续三维图片云中，不产生无限横向画布，也不使用离散翻页。
- 每张图片拥有独立的 X/Y/Z 空间坐标，不按批次或楼层堆叠；相机能够靠近、放大并穿过图片继续浏览后方内容。
- 按住画布任意位置（包括图片本体）拖动可环顾四周；滚轮向前时镜头前进、向后时镜头后退，上下方向键保持相同语义；左右方向键改变观察方向。
- 图片按距离连续缩放和淡入淡出，并始终保持正向可读；悬停放大，双击进入详情。
- 滚轮移动必须沿当前镜头视线：视角朝上时向上推进，朝下时向下推进，转向后向该方向推进。
- 开始一次滚轮手势时记录当前镜头视线并沿该方向自然移动；不吸附或强制居中任何图片，图片可以从视野任意位置靠近并被穿过。
- 远处图片只承担空间层次，不响应鼠标；图片进入近距离清晰范围后才允许拖动、收藏、选择和打开。
- 为避免图片移动与相机环顾冲突，拖动图片位置只通过悬停工具栏中的移动手柄触发；图片本体拖动始终用于环顾。
- 空间首页直接显示本地原图并异步解码，避免压缩缩略图在透视缩放后变糊。
- 空间首页的图片容器保持透明且不加投影或半透明衬底，只呈现图片本身。
- 首页空间交互参考 Studio Olafur Eliasson 的 Your uncertain archive，但只复用相机与纵深机制，不复制其内容或品牌素材。
- 图片自身保留全部色彩，UI 标签保持中性。

### 图片详情

- 顶部沿用首页的全宽深灰搜索栏，搜索范围只包含本机图库；输入时在搜索栏下展示可直接打开的图片结果。
- 参考 Pinterest 的信息层级：主图与编辑信息组成完整的大卡片，相关推荐在右侧与主卡片下方形成两条彼此独立的连续图片流。
- 详情内容使用 6 条等宽轨道和统一 20px 间距：左侧主内容占 4 条轨道，右侧推荐占 2 条；主卡片结束后，左侧推荐立即接续，不等待右侧内容结束。
- 主图容器与相关图片使用 22–28px 圆角，图片比例由原图决定，不强行裁成相同高度。
- 详情主卡片使用稳定的浏览高度与受控的主图宽度；超长主图在图片区域内部上下滚动，信息区域单独滚动，不能无限撑高整页。
- 相关推荐按 1:2 分配至右侧双列与左侧四列，并根据图片宽高估算当前较短列；两处卡片列宽与间距一致，任何一条长列都不能撑开另一条内容流。
- 收藏、文件位置、删除、重新分析必须保持清晰的主次和危险语义。
- 已有设计术语的图片显示“AI 分析已完成”，后台与重启恢复不得再次分析；只有用户主动点击“重新分析”才允许覆盖分析该图。

### 周手账

- 保留周历三列两行和全宽笔记区。
- 日期格使用极浅纸张纹理和宽松留白，不使用应用级蓝色装饰画面。
- 图片默认采用稳定的随性拼贴。依据图片哈希生成 38%-46% 的适度宽度差、小范围错位与不超过 2.4 度的旋转，刷新后保持不变。
- 拼贴采用紧凑纵向节奏与约 3% 的列间基础留白，避免图片之间出现大片无效空白。
- 同列卡片必须保留安全的纵向间距，默认不互相遮挡；用户手动拖动与缩放继续叠加在默认排布上。
- 图钉、回形针、胶带和贴纸继续使用珊瑚红、钴蓝、薄荷绿、紫色、粉色、柠檬黄、天蓝和橘色。
- 术语标签叠放在图片上，悬停展开；图片继续支持拖动与缩放。

### 数据与设置

- 顶部使用水平分段导航。
- 内容使用 12 列 dense grid；宽模块占 8 或 12 列，辅助模块占 4 或 6 列，任何一行不得留下无意义空角。
- 统计数据不套多层卡片；通过数字尺寸和浅灰章节面形成层级。
- Gemini 密钥只显示保存状态，不回显内容，不进入备份。
- 自动分析采用单列状态模块，直接展示等待、分析中和失败数量；只提供“重试全部失败”以及“暂停分析/继续分析”两个明确动作。
- 队列状态沿用现有中性色、圆角与胶囊按钮体系；日间和夜间使用同一信息层级，不增加进度条或装饰动画。

### 桌宠

- 桌宠窗口继续完全透明，不增加卡片底、边框或背景。
- 吃图动画继续使用透明 PNG；提示气泡采用中性白/黑表面。

## 7. 组件状态

- Primary：蓝底白字的全圆角按钮，每个内容章节最多一个主要操作。
- Secondary：浅灰底深色字，无边框。
- Ghost：透明背景，悬停时出现浅灰面。
- Danger：危险红色文字或浅红底，不与主要蓝色操作并列抢权重。
- 输入框：浅灰底、无常驻边框；聚焦时显示 2px 蓝色焦点环。
- Loading：使用与最终内容形状一致的骨架，避免通用旋转圈。
- Empty：宽松排版，解释如何导入第一张图片，只有一个主要行动。

## 8. 动效

- 页面进入：约 0.45 秒，从 14px 下方淡入；使用 GSAP，减少动画时禁用。
- 图片层级：使用弹簧或缓动完成缩放与透明度过渡。
- 图片悬停：最大放大 1.04，不旋转、不制造大阴影。
- 详情相关灵感：滚动进入时轻微缩放和淡入；不劫持正常滚动。
- 所有动效不得阻挡删除、保存、导航等基础操作。

## 9. 验收清单

- [ ] 全部图片、图片详情、周手账、数据与设置、抽屉与空状态使用同一套 token。
- [ ] 应用 UI 中不再出现旧琥珀色强调色。
- [ ] 多彩手账装饰完整保留。
- [ ] 所有功能图标来自 Phosphor，不以字符或表情代替。
- [ ] 卡片圆角统一为 28px，按钮统一为全圆角。
- [ ] 页面没有非必要阴影、水平滚动条和不可读按钮。
- [ ] 日间、夜间和减少动画模式均可用。
- [ ] Gemini API 密钥仍仅保存在 Windows 本机加密存储中。
